# Кластер Up — patronictl и CRLF

Контейнеры работают. Ошибка только в `patronictl` (CRLF в `patronictl.py` на Windows).

## Сейчас — проверить кластер (работает без rebuild)

```powershell
docker exec demo-patroni1 python3 /patronictl.py list
```

Должно показать 3 ноды и Leader.

## Починить patronictl навсегда

```powershell
cd C:\Users\User\CppProjects\system-design-hse-miem-2026
git pull

cd code\postgres-ha
.\fix-line-endings.ps1

cd patroni-master
docker build -f Dockerfile.offline --build-arg PG_MAJOR=15 -t patroni .

cd ..
docker compose up -d --force-recreate demo-patroni1 demo-patroni2 demo-patroni3
Start-Sleep -Seconds 60
docker exec demo-patroni1 patronictl list
```

Без `--no-cache` — быстрее, vendor уже есть.

## Дальше по практике HW2

```powershell
Get-Content init-schema.sql | docker exec -i demo-patroni1 psql -U postgres -h haproxy -p 5001 -d postgres
```
