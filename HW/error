# Сейчас: confd в репозитории — pull на Windows и сборка

`vendor/confd` уже в git (скачан на Mac). `etcd.tar.gz` на Windows уже должен быть локально (~10 MB).

## Windows — одна команда

```powershell
cd C:\Users\User\CppProjects\system-design-hse-miem-2026
git pull

cd code\postgres-ha
.\fix-and-rebuild.ps1
```

Если скрипт скажет что нет `etcd.tar.gz`:

```powershell
dir patroni-master\vendor
```

Если нет etcd — скачать в браузере:
https://github.com/coreos/etcd/releases/download/v3.3.13/etcd-v3.3.13-linux-amd64.tar.gz
→ `patroni-master\vendor\etcd.tar.gz`

Потом снова `.\fix-and-rebuild.ps1`
